{
  "name": "rate_limiter_service",
  "version": "1.0.0",
  "description": "API Throttling / Rate Limiting with sliding window counter algorithm",
  "main": "server.js",
  "scripts": {
    "start": "set NODE_ENV=dev && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "API",
    "Throttling",
    "Rate",
    "limiting"
  ],
  "author": "Jasim",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "moment": "^2.29.4",
    "redis": "^4.6.5"
  },
  "devDependencies": {
    "loadtest": "^5.2.0"
  }
}
